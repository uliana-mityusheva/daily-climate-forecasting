defaults:
  - data: data
  - model: model
  - train: train
  - predict: predict
  - export: export
  - logging: logging
  - serve: serve
  - _self_

# Global config anchor for future extensions (logging/serve, etc.)
